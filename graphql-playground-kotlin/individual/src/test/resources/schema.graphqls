type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author,
    company: Company,
    publishedAt(dateFormat : String = "dd, MMM, yyyy"): String
}

type Author {
    id: ID
    firstName: String
    lastName: String
}

type Company {
    name: String
    address: String
}
